import 'package:flutter/material.dart';

class AppColors {
  // Cor principal do app - o Material 3 gera o resto automaticamente
  static const lightBlue = Color(0xFF43C8E6);
  
  // Cores extras caso precise usar diretamente
  static const darkBlue = Color(0xFF004697);
  static const mediumBlue = Color(0xFF008ED5);
}
